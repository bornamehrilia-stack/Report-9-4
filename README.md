# Report-9-4


---

# 📋 گزارش پروژه: کنترل LED با وقفه (External Interrupt on Arduino)

## ۱. مقدمه  
در این پروژه, یک **دکمه** به پین دیجیتال ۲ آردوینو متصل شده و با فشار دادن آن, حالت یک LED (پین ۱۳) بلافاصله تغییر می‌کند — بدون اینکه نیاز به چک کردن حلقه `loop()` باشد. این امکان با استفاده از **وقفه خارجی (External Interrupt)** فراهم می‌شود، که یکی از قدرتمندترین ویژگی‌های میکروکنترلرها برای پاسخ‌گویی فوری به رویدادهای خارجی است.
.


## ۳. قطعات مورد استفاده  
- برد آردوینو Uno  
- یک دکمه فشرده‌شونده (Push Button)  
- یک مقاومت pull-up (مثلاً 10k اهم) — *اختیاری، چون از `INPUT_PULLUP` استفاده شده*  
- برد برد و سیم‌های جامپر  



## ۵. کد برنامه (اسکچ)

```cpp
int pin = 2;                     // پین دکمه (پین وقفه)
volatile int state = LOW;        // حالت LED — حتماً volatile برای توابع وقفه

void setup() {
  Serial.begin(9600);
  pinMode(pin, INPUT_PULLUP);    // تنظیم پین دکمه با مقاومت pull-up داخلی
  pinMode(13, OUTPUT);          // تنظیم پین LED (onboard)
  
  // اتصال وقفه: هنگامی که پین 2 از HIGH به LOW تغییر کند (FALLING)، تابع blink فراخوانی شود
  attachInterrupt(digitalPinToInterrupt(pin), blink, FALLING);
}

void loop() {
  digitalWrite(13, state);      // نمایش حالت فعلی LED
  Serial.println(state);        // نمایش حالت در Serial Monitor
  delay(100);                 // تأخیر برای ثبات — اما تأثیری بر پاسخ وقفه ندارد!
}

// تابع وقفه — باید سریع و بدون delay باشد
void blink() {
  state = !state;  // معکوس کردن حالت (LOW → HIGH, HIGH → LOW)
}
```.

## ۶. نحوه‌ی کارکرد  
1. پس از آپلود کد, LED روشن یا خاموش است (بر اساس حالت اولیه `state = LOW`).  
2. وقتی دکمه فشرده می‌شود (پین 2 از HIGH به LOW می‌رود):  
   - وقفه فوراً فعال می‌شود  
   - تابع `blink()` اجرایی می‌شود  
   - `state` معکوس می‌شود  
   - در حلقه `loop()`, `digitalWrite(13, state)` حالت جدید را اعمال می‌کند  
3. این تغییر **بی‌تأخیر** است — حتی اگر `loop()` در حال اجرای `delay(5000)` باشد!

## ۷. نتایج  
- LED به صورت فوری و بدون تأخیر، هنگام فشردن دکمه، تغییر حالت می‌دهد.  
---

